# Frontend environment configuration
VITE_API_URL=http://localhost:3001/api

# Optional: Enable auto-fill OTP in dev mode (default: false)
# Set to 'true' to auto-generate and pre-fill OTP on signup verification step
VITE_DEV_AUTO_FILL_OTP=false
