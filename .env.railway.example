# Railway Backend Environment Configuration
# Copy these to Railway Dashboard > Your App > Variables

# Database Configuration (MySQL)
DB_HOST=your-mysql-host.railway.app
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your-mysql-password
DB_NAME=apuic_capital

# Node Environment
NODE_ENV=production
PORT=3000

# Optional: JWT Secret
JWT_SECRET=your-super-secret-key-change-this

# Optional: Cron/Jobs
ENABLE_CRON_JOBS=true
